---
phase: 01-cenarios-e-carta-fianca
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/lib/pdf.js
  - src/lib/whatsapp.js
  - src/components/HistoryModal.jsx
autonomous: true
requirements: [R5, R1]
must_haves:
  truths:
    - "PDF exibe 'Custo inicial' (não mais 'Total à vista') e inclui linha 'Carta de fiança'"
    - "WhatsApp exibe 'Custo inicial' e inclui linha 'Carta de fiança'"
    - "HistoryModal mostra badge 'Cenários (X%)' ou 'Arremate fixo' por item"
    - "Cálculos antigos (sem mode/surety) renderizam sem crash"
  artifacts:
    - path: "src/lib/pdf.js"
      provides: "PDF com Custo inicial + linha de fiança"
      contains: "Custo inicial"
    - path: "src/lib/whatsapp.js"
      provides: "Mensagem com Custo inicial + linha de fiança"
      contains: "Custo inicial"
    - path: "src/components/HistoryModal.jsx"
      provides: "Badge de modo no histórico"
      contains: "Arremate fixo"
  key_links:
    - from: "src/components/HistoryModal.jsx"
      to: "calc.mode / calc.discountPct"
      via: "render condicional do badge"
      pattern: "mode\\s*===\\s*['\"]scenarios['\"]"
---

<objective>
Aplicar a renomeação "Total à vista" → "Custo inicial" e adicionar a linha "Carta de fiança" nas saídas PDF e WhatsApp (R5, R1). Adicionar badge de modo no HistoryModal (D-07).

Purpose: Estes três arquivos são consumidores da forma calculada e não conflitam com o trabalho do App.jsx (Plan 05). Podem rodar paralelos ao Plan 03.
Output: PDF, WhatsApp e History atualizados; cálculos antigos lidos com defaults.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-cenarios-e-carta-fianca/01-SPEC.md
@.planning/phases/01-cenarios-e-carta-fianca/01-CONTEXT.md
@src/lib/pdf.js
@src/lib/whatsapp.js
@src/components/HistoryModal.jsx
@src/lib/format.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: pdf.js — renomear + linha de fiança</name>
  <files>src/lib/pdf.js</files>
  <read_first>
    - src/lib/pdf.js (estrutura completa: layout do PDF, ordem de linhas, helpers de format)
    - src/lib/calc.js (campos disponíveis após Plan 01: surety, suretyPct)
  </read_first>
  <action>
    Per R5 + R1 + D-11:
    1. Substituir TODA ocorrência de "Total à vista" por "Custo inicial". Substituir sublabel "Entrada 25% + comissão X%" (ou similar) por "Entrada + comissão + carta de fiança".
    2. Adicionar nova linha "Carta de fiança" no breakdown, posicionada ENTRE "Comissão" e "Custo inicial". Renderizar como `brl(calc.surety ?? 0)`. Se `calc.surety` ausente (cálculos antigos), exibir `R$ 0,00` — não esconder a linha (default consistente).
    3. Garantir que o valor de "Custo inicial" lido do `calc.upfront` continua correto (Plan 01 já garante upfront = entry + commission + surety).
  </action>
  <verify>
    <automated>grep -c "Total à vista" src/lib/pdf.js; grep -c "Custo inicial\\|Carta de fiança" src/lib/pdf.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Total à vista" src/lib/pdf.js === 0
    - grep -c "Custo inicial" src/lib/pdf.js >= 1
    - grep -c "Carta de fiança" src/lib/pdf.js >= 1
    - grep -c "calc\\.surety\\|surety" src/lib/pdf.js >= 1
    - npm run build passa
  </acceptance_criteria>
  <done>PDF mostra Custo inicial + linha de carta; build verde.</done>
</task>

<task type="auto">
  <name>Task 2: whatsapp.js — renomear + linha de fiança</name>
  <files>src/lib/whatsapp.js</files>
  <read_first>
    - src/lib/whatsapp.js (template da mensagem)
  </read_first>
  <action>
    Per R5 + R1 + D-11: mesmo tratamento do Task 1, aplicado ao texto do WhatsApp:
    - "Total à vista" → "Custo inicial" em todas as ocorrências
    - Adicionar linha `*Carta de fiança:* ${brl(calc.surety ?? 0)}` (manter o pattern de formatação Markdown-WhatsApp já usado no arquivo) entre comissão e custo inicial
    - Sublabel/legenda se existir: "Entrada + comissão + carta de fiança"
  </action>
  <verify>
    <automated>grep -c "Total à vista" src/lib/whatsapp.js; grep -c "Custo inicial\\|Carta de fiança" src/lib/whatsapp.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Total à vista" src/lib/whatsapp.js === 0
    - grep -c "Custo inicial" src/lib/whatsapp.js >= 1
    - grep -c "Carta de fiança" src/lib/whatsapp.js >= 1
  </acceptance_criteria>
  <done>WhatsApp text inclui custo inicial + carta.</done>
</task>

<task type="auto">
  <name>Task 3: HistoryModal — badge de modo + sanitização do label</name>
  <files>src/components/HistoryModal.jsx</files>
  <read_first>
    - src/components/HistoryModal.jsx (estrutura do card de histórico)
    - .planning/phases/01-cenarios-e-carta-fianca/01-CONTEXT.md (D-06, D-07)
  </read_first>
  <action>
    Per D-07 + D-11:
    1. Se houver qualquer texto "Total à vista" → trocar por "Custo inicial".
    2. Adicionar badge no card de cada item do histórico:
       - Se `item.calc.mode === 'scenarios'`: badge texto = `Cenários (${item.calc.discountPct}%)`
       - Se `item.calc.mode === 'fixed'` ou ausente (cálculos antigos): badge texto = `Arremate fixo`
       - Estilo: pequena pill, reutilizar classes existentes (`text-xs`, `px-2 py-0.5 rounded-full`, cores sóbrias do tema). Posição sugerida: header do card, junto ao timestamp/comprador.
    3. Se o card mostrar valor de "custo inicial" / "total à vista", usar `item.calc.upfront ?? 0` e label "Custo inicial".
  </action>
  <verify>
    <automated>grep -c "Cenários\\|Arremate fixo" src/components/HistoryModal.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Total à vista" src/components/HistoryModal.jsx === 0
    - grep -c "Arremate fixo" src/components/HistoryModal.jsx >= 1
    - grep -c "Cenários" src/components/HistoryModal.jsx >= 1
    - grep -E "mode\\s*===\\s*['\"]scenarios['\"]|calc\\.mode" src/components/HistoryModal.jsx
    - npm run build passa
  </acceptance_criteria>
  <done>Cada item do histórico tem badge correto; cálculos antigos exibem "Arremate fixo".</done>
</task>

</tasks>

<verification>
- grep -ri "Total à vista" src/lib/pdf.js src/lib/whatsapp.js src/components/HistoryModal.jsx → 0
- PDF gerado contém "Custo inicial" e "Carta de fiança"
- HistoryModal renderiza badge correto para mode fixed e scenarios
</verification>

<success_criteria>
- Saídas externas (PDF/WA) refletem nova terminologia + carta
- Histórico distingue visualmente modo do cálculo
</success_criteria>

<output>
After completion, create `.planning/phases/01-cenarios-e-carta-fianca/01-04-SUMMARY.md`
</output>
