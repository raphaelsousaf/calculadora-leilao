---
phase: 01-cenarios-e-carta-fianca
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - src/App.jsx
autonomous: false
requirements: [R2, R3, R4, R5]
must_haves:
  truths:
    - "Usuário vê segmented control 'Arremate fixo' / 'Simular por avaliação' no topo do form"
    - "Modo escolhido persiste em localStorage chave 'calc.lastMode'"
    - "Trocar de modo preserva meta/comissão/parcelas/fiança; só zera input exclusivo"
    - "Campo 'Carta de fiança (%)' override fica no grid principal sempre visível"
    - "Default da fiança vem de Settings.defaultSuretyPct, fallback 1%"
    - "No modo cenários: input avaliação renderiza tabela desktop (8 colunas) ou cards mobile (<768px) com 13 linhas"
    - "Linhas com pct entre 70 e 85 inclusive têm fundo --zone-ideal-bg"
    - "Clicar linha/card seleciona o cenário e popula o painel Resumo + habilita PDF/WhatsApp/Salvar"
    - "Caso canônico: avaliação 61000, com 5%, fia 1%, 30x, linha 70% → painel Resumo mostra arremate 42700, custo inicial 13237, parcela 1067.50"
    - "Hero-stat exibe 'Custo inicial' com sublabel 'Entrada + comissão + carta de fiança'"
    - "calc salvo via insertCalculation contém { mode, appraisal?, discountPct?, suretyPct, surety }"
    - "handleLoad restaura mode/suretyPct e (em scenarios) appraisal+discountPct do histórico — acceptance criterion 10 do SPEC"
    - "Carregar item legado (sem mode no calc) preenche modo 'fixed' e suretyPct=0 sem erro"
  artifacts:
    - path: "src/App.jsx"
      provides: "Toggle, override fiança, modo cenários, matriz desktop+mobile, click-to-select, renomeação completa, handleLoad estendido"
      contains: "buildScenarioMatrix"
  key_links:
    - from: "src/App.jsx"
      to: "src/lib/scenarios.js"
      via: "import { buildScenarioMatrix, IDEAL_ZONE }"
      pattern: "from ['\"]\\./lib/scenarios"
    - from: "src/App.jsx"
      to: "src/lib/calc.js"
      via: "calculate({ ..., suretyPct })"
      pattern: "suretyPct"
    - from: "src/App.jsx"
      to: "localStorage"
      via: "getItem/setItem('calc.lastMode')"
      pattern: "calc\\.lastMode"
    - from: "src/App.jsx handleLoad"
      to: "histórico calc.{mode,suretyPct,appraisal,discountPct}"
      via: "leitura no handler"
      pattern: "item\\.calc\\.(mode|suretyPct|appraisal|discountPct)"
---

<objective>
Wire de tudo no `App.jsx`: toggle de modo (D-01..D-03), override de fiança (D-08), modo cenários com matriz responsiva e click-to-select (R3, R4, D-04, D-05), realce de zona ideal (D-09), persistência estendida (D-06), handleLoad estendido para restaurar todos os campos novos do cálculo (acceptance criterion 10 do SPEC), e finalizar a renomeação "Total à vista" → "Custo inicial" no hero (R5, D-11).

Purpose: É a integração final visível ao usuário. Todos os outros planos preparam dependências; aqui orquestramos.
Output: App.jsx com dois modos funcionais, matriz interativa, histórico round-trip completo e UX completa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-cenarios-e-carta-fianca/01-SPEC.md
@.planning/phases/01-cenarios-e-carta-fianca/01-CONTEXT.md
@src/App.jsx
@src/lib/calc.js
@src/lib/scenarios.js
@src/lib/format.js
@src/lib/data.js
@src/index.css

<interfaces>
<!-- Disponível após Wave 1+2 -->
```js
// src/lib/calc.js
calculate({ arremate, commissionPct, suretyPct, installments }) ->
  { bid, commissionPct, suretyPct, installments, entry, commission, surety, upfront, remaining, installment, total }

// src/lib/scenarios.js
DEFAULT_SCENARIO_PCTS // [30,35,50,55,60,65,70,75,80,85,88,90,100]
IDEAL_ZONE // { min: 70, max: 85 }
buildScenarioMatrix({ appraisal, commissionPct, suretyPct, installments }) ->
  Array<{ pct, bid, entry, remaining, commission, surety, upfront, installment, isIdeal }>

// src/lib/data.js
fetchSettings(userId) -> { ..., defaultSuretyPct: number | null | undefined }
insertCalculation(userId, { calc, meta }) -> { id, savedAt, calc, meta }

// src/lib/format.js
parseBRL, formatBRLInput, brl, pct
```

<!-- calc payload schema (D-06) -->
```js
// O que vai dentro de `calc` ao salvar
{
  // legados (sempre)
  bid, commissionPct, installments, entry, commission, upfront, remaining, installment, total,
  // novos
  mode: 'fixed' | 'scenarios',
  suretyPct: number,   // sempre presente após esta fase
  surety: number,      // sempre presente
  appraisal?: number,  // só em scenarios
  discountPct?: number // só em scenarios
}
```

<!-- handleLoad atual em src/App.jsx (linhas ~108-114) — base do diff de Task 3 -->
```js
const handleLoad = (item) => {
  setArremateStr(formatBRLInput(String(Math.round((item.calc.bid || 0) * 100))))
  setCommissionPct(item.calc.commissionPct ?? DEFAULT_COMMISSION)
  setInstallments(item.calc.installments ?? DEFAULT_INSTALLMENTS)
  setMeta({ ...EMPTY_META, ...item.meta })
  showToast('Cálculo carregado')
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toggle de modo + override de fiança + estado base</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (componente Calculator: useState atuais, layout do form, hero-stat, painel resumo, save/PDF/WhatsApp handlers)
    - src/lib/scenarios.js (após Plan 01)
    - src/lib/data.js (após Plan 02)
    - .planning/phases/01-cenarios-e-carta-fianca/01-CONTEXT.md (D-01..D-11)
  </read_first>
  <action>
    Implementar per D-01, D-02, D-03, D-08, D-10:

    1. **Estado novo:**
       - `const [mode, setMode] = useState(() => localStorage.getItem('calc.lastMode') || 'fixed')` — D-03
       - `const [appraisalStr, setAppraisalStr] = useState('')` (input do modo cenários)
       - `const [suretyPctStr, setSuretyPctStr] = useState('')` (override; vazio = usa default de Settings; '0' válido)
       - `const [selectedDiscountPct, setSelectedDiscountPct] = useState(null)` (linha selecionada na matriz)
       - Effect: `useEffect(() => localStorage.setItem('calc.lastMode', mode), [mode])`

    2. **Carregar default de fiança de Settings:** ao receber settings via fetchSettings (já existe handler/useEffect), sincronizar `suretyPctStr` UMA VEZ se ainda vazio: se `settings.defaultSuretyPct != null && suretyPctStr === ''`, set `String(settings.defaultSuretyPct)`. Se settings vier sem default, usar `'1'` (fallback global). Não sobrescrever se usuário já editou.

    3. **suretyPct derivado:**
       ```js
       const suretyPct = useMemo(() => {
         if (suretyPctStr.trim() === '') return settings.defaultSuretyPct ?? 1
         const n = Number(suretyPctStr)
         return Number.isFinite(n) ? n : 0
       }, [suretyPctStr, settings.defaultSuretyPct])
       ```

    4. **Segmented control** (componente inline com Tailwind, não criar novo arquivo) — renderizado no topo do `<section className="card p-5 sm:p-7 space-y-6">` dos inputs, ANTES do label "Valor de arremate":
       ```jsx
       <div role="tablist" aria-label="Modo de cálculo" className="inline-flex rounded-lg border ...">
         <button role="tab" aria-selected={mode==='fixed'} onClick={() => setMode('fixed')}>Arremate fixo</button>
         <button role="tab" aria-selected={mode==='scenarios'} onClick={() => setMode('scenarios')}>Simular por avaliação</button>
       </div>
       ```
       Estilo: pill ativa com `bg-primary text-white`, inativa transparente. Acessível via teclado.

    5. **Trocar de modo (D-02):** ao chamar `setMode(novo)`, NÃO zerar nada exceto:
       - Indo para `scenarios`: zerar `arremateStr` e `selectedDiscountPct` (vai vir da matriz)
       - Indo para `fixed`: zerar `appraisalStr` e `selectedDiscountPct`
       - Preservar: meta (comprador, lote, etc.), `commissionPct`, `installments`, `suretyPctStr`.

    6. **Grid principal de inputs (D-08):** reorganizar o grid existente para `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` contendo, nesta ordem: Comissão (%) | Carta de fiança (%) | Parcelas. Campo de fiança:
       - label="Carta de fiança (%)"
       - sublabel exato: "Use 0 se o edital não exigir."
       - type="number" step="0.1" min="0" max="100" placeholder={String(settings.defaultSuretyPct ?? 1)}
       - value={suretyPctStr}, onChange={e => setSuretyPctStr(e.target.value)}
       - Fica fora do `<details>`, sempre visível.

    7. **Renomeação no hero-stat (R5, D-11):** trocar "Total à vista" / "Total à vista (no dia)" pelo label "Custo inicial". Sublabel: "Entrada + comissão + carta de fiança". Valor lido continua sendo `calc.upfront` (Plan 01 já incluiu surety).

    8. **Cálculo ativo** (`calc` usado pelo painel Resumo): em modo `fixed`, `calculate({ arremate: parseBRL(arremateStr), commissionPct, suretyPct, installments })`. Em modo `scenarios`, se `selectedDiscountPct == null` → mostrar matriz mas painel Resumo desabilitado (sem botões PDF/WA/Salvar). Se selecionado → `calculate({ arremate: parseBRL(appraisalStr) * (selectedDiscountPct/100), commissionPct, suretyPct, installments })`.

    9. **Botões PDF/WhatsApp/Salvar:** desabilitar em modo `scenarios` enquanto `selectedDiscountPct == null`. Ao salvar (`insertCalculation`), montar o `calc` payload incluindo:
       ```js
       { ...calculate(...), mode, suretyPct, surety: <calc>.surety,
         ...(mode === 'scenarios' ? { appraisal: parseBRL(appraisalStr), discountPct: selectedDiscountPct } : {}) }
       ```

    Importações novas no topo:
    ```js
    import { buildScenarioMatrix, IDEAL_ZONE } from './lib/scenarios'
    ```
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "calc\\.lastMode" src/App.jsx >= 2 (read + write)
    - grep -c "setMode" src/App.jsx >= 2
    - grep -c "Arremate fixo" src/App.jsx >= 1
    - grep -c "Simular por avaliação" src/App.jsx >= 1
    - grep -c "Carta de fiança" src/App.jsx >= 1
    - grep -c "Use 0 se o edital não exigir" src/App.jsx >= 1
    - grep -c "Custo inicial" src/App.jsx >= 1
    - grep -c "Total à vista" src/App.jsx === 0
    - grep -c "Entrada + comissão + carta de fiança" src/App.jsx >= 1
    - grep -c "suretyPct" src/App.jsx >= 3
    - grep -c "grid-cols-1 md:grid-cols-2 lg:grid-cols-3" src/App.jsx >= 1
    - grep -c "from ['\"]./lib/scenarios" src/App.jsx >= 1
    - npm run build exits 0
  </acceptance_criteria>
  <done>Toggle, override de fiança, renomeação e payload de save funcionam; mode persiste em localStorage.</done>
</task>

<task type="auto">
  <name>Task 2: Modo cenários — input avaliação + matriz responsiva + click-to-select</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (após Task 1 desta plan)
    - src/lib/scenarios.js
    - src/index.css (tokens --zone-ideal-bg / --zone-ideal-fg)
    - .planning/phases/01-cenarios-e-carta-fianca/01-SPEC.md (caso canônico R3)
  </read_first>
  <action>
    Per R3, R4, D-04, D-05, D-09:

    1. **Bloco condicional `mode === 'scenarios'`:** substitui (visualmente) o input de arremate. Renderizar:
       - Input "Valor de avaliação" com `formatBRLInput`, value={appraisalStr}, onChange seta `appraisalStr` e zera `selectedDiscountPct`.
       - Abaixo: matriz, calculada via `useMemo`:
         ```js
         const scenarios = useMemo(() => buildScenarioMatrix({
           appraisal: parseBRL(appraisalStr),
           commissionPct, suretyPct, installments
         }), [appraisalStr, commissionPct, suretyPct, installments])
         ```

    2. **Layout responsivo (D-04, D-05):**
       - Wrapper: `<div className="hidden md:block">{tabela}</div><div className="md:hidden space-y-3">{cards}</div>`
       - **Desktop (≥768px):** `<table>` com `overflow-x-auto`. Header em ordem locked: `% | Arrematação | Entrada | Saldo | Comissão | Carta fiança | Custo inicial | Parcela`. Cada linha (`<tr>`) é clicável (`role="button"`, `tabIndex={0}`, `onClick`, `onKeyDown` Enter/Space) chamando `setSelectedDiscountPct(row.pct)`.
       - **Mobile (<768px):** card-por-cenário empilhado. Cada card mostra `pct` em destaque e os 7 demais campos em grid 2 colunas (label/value). Mesmo handler de click no `<button>` wrappando cada card.

    3. **Realce zona ideal (D-09):** linhas/cards com `row.isIdeal === true` recebem inline style:
       ```jsx
       style={row.isIdeal ? {
         background: 'rgb(var(--zone-ideal-bg))',
         color: 'rgb(var(--zone-ideal-fg))'
       } : undefined}
       ```
       (Tokens introduzidos pelo Plan 01 Task 3.)

    4. **Linha selecionada:** se `row.pct === selectedDiscountPct`, adicionar ring/border destacado (ex.: `ring-2 ring-primary`) — empilhável com o background da zona ideal.

    5. **Formatação:** todos os valores numéricos via `brl(...)`; `pct` exibido como `${row.pct}%`.

    6. **Acessibilidade:** tabela tem `<caption className="sr-only">Cenários de desconto</caption>`. Cards têm `aria-pressed={row.pct === selectedDiscountPct}`.

    7. **Painel Resumo (já existente):** quando `mode === 'scenarios' && selectedDiscountPct != null`, populado pelo `calc` derivado (Task 1 item 8). Quando `selectedDiscountPct == null`, exibir mensagem "Selecione um cenário na matriz" e desabilitar botões.

    Caso de teste manual durante implementação: avaliação `R$ 61.000,00`, comissão `5`, fiança `1`, parcelas `30` → linha 70% deve mostrar Arrematação=R$42.700,00, Entrada=R$10.675,00, Saldo=R$32.025,00, Comissão=R$2.135,00, Fiança=R$427,00, Custo inicial=R$13.237,00, Parcela=R$1.067,50. (Validação automatizada já está em scenarios.test.js.)
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "buildScenarioMatrix" src/App.jsx >= 1
    - grep -c "selectedDiscountPct" src/App.jsx >= 4
    - grep -c "appraisalStr" src/App.jsx >= 3
    - grep -c "Valor de avaliação" src/App.jsx >= 1
    - grep -c "hidden md:block" src/App.jsx >= 1 (table desktop)
    - grep -c "md:hidden" src/App.jsx >= 1 (cards mobile)
    - grep -c "zone-ideal-bg" src/App.jsx >= 1
    - grep -E "Arrematação|Entrada|Saldo|Comissão|Carta fiança|Custo inicial|Parcela" src/App.jsx | wc -l >= 7
    - grep -c "isIdeal" src/App.jsx >= 1
    - npm run build exits 0
  </acceptance_criteria>
  <done>Modo cenários funcional: input avaliação + matriz desktop/mobile + zona ideal + click seleciona + painel resumo populado.</done>
</task>

<task type="auto">
  <name>Task 3: Estender handleLoad para round-trip completo do histórico (acceptance criterion 10 do SPEC)</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx linhas ~108-114 (handleLoad atual — ver bloco em &lt;interfaces&gt; acima)
    - src/App.jsx (estado introduzido em Task 1: setMode, setSuretyPctStr, setAppraisalStr, setSelectedDiscountPct + EMPTY_META, DEFAULT_COMMISSION, DEFAULT_INSTALLMENTS)
    - src/components/HistoryModal.jsx (entender que `item` tem `{ id, savedAt, calc, meta }` — `calc` é o payload schema do bloco &lt;interfaces&gt; D-06)
    - .planning/phases/01-cenarios-e-carta-fianca/01-SPEC.md (acceptance criterion 10: "Histórico aceita salvar e recarregar um cálculo do modo 'avaliação → cenários' sem perda de informação do cenário escolhido")
  </read_first>
  <action>
    Per acceptance criterion 10 do SPEC + D-06: estender `handleLoad` em `src/App.jsx` (handler atual nas linhas ~108-114) para restaurar TODOS os campos novos persistidos no payload de save (Task 1 item 9 desta plan).

    Diff alvo (substituir o handler atual):
    ```js
    const handleLoad = (item) => {
      const c = item.calc || {}

      // Legados (já existiam)
      setArremateStr(formatBRLInput(String(Math.round((c.bid || 0) * 100))))
      setCommissionPct(c.commissionPct ?? DEFAULT_COMMISSION)
      setInstallments(c.installments ?? DEFAULT_INSTALLMENTS)
      setMeta({ ...EMPTY_META, ...item.meta })

      // Novos campos (D-06) — backwards-compat para itens legados sem mode/suretyPct
      const loadedMode = c.mode ?? 'fixed'
      setMode(loadedMode)
      // suretyPct: itens legados (sem o campo) carregam como 0 sem erro.
      // Itens novos restauram o valor exato salvo (override do usuário no momento do save).
      setSuretyPctStr(c.suretyPct != null ? String(c.suretyPct) : '0')

      if (loadedMode === 'scenarios') {
        // Restaurar input de avaliação e seleção da matriz
        setAppraisalStr(formatBRLInput(String(Math.round((c.appraisal || 0) * 100))))
        setSelectedDiscountPct(c.discountPct ?? null)
      } else {
        // Modo fixo: limpar campos exclusivos de scenarios para evitar estado órfão
        setAppraisalStr('')
        setSelectedDiscountPct(null)
      }

      showToast('Cálculo carregado')
    }
    ```

    Notas de implementação:
    - **Backwards-compat:** itens salvos antes desta fase não têm `mode`, `suretyPct`, `appraisal`, `discountPct`. Default seguro: `mode='fixed'`, `suretyPct=0` (sem fiança), sem appraisal/discountPct. Não pode quebrar.
    - **Ordem dos `set*`:** React lota tudo no próximo render, mas é importante que `setMode` venha antes do branch para evitar flicker de modo errado. (Em prática React batcha; a ordem visual no código facilita revisão.)
    - **Não alterar `handleSave`:** o payload de save já é montado em Task 1 item 9; este task apenas consome o que já é salvo.
    - Não modificar `HistoryModal.jsx` — o componente passa o `item` inteiro pra cá.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/App.jsx handleLoad reads item.calc.mode (grep -c "item\\.calc\\.mode\\|c\\.mode" src/App.jsx >= 1)
    - src/App.jsx handleLoad reads item.calc.suretyPct (grep -c "c\\.suretyPct\\|item\\.calc\\.suretyPct" src/App.jsx >= 1)
    - src/App.jsx handleLoad branches on mode === 'scenarios' (grep -c "loadedMode === 'scenarios'\\|mode === 'scenarios'" src/App.jsx >= 1 dentro do handleLoad)
    - src/App.jsx handleLoad chama setMode (grep -c "setMode(" src/App.jsx >= 3 — Task 1 traz 2, esta traz 1)
    - src/App.jsx handleLoad chama setSuretyPctStr (grep -c "setSuretyPctStr(" src/App.jsx >= 2 — Task 1 traz 1, esta traz 1)
    - src/App.jsx handleLoad chama setAppraisalStr e setSelectedDiscountPct (presença em ambos os branches do if/else)
    - npm run build exits 0
    - Smoke teste manual (validado no checkpoint Task 4): carregar item legado (sem `mode` no calc) NÃO crasha e preenche modo='fixed', fiança='0'.
  </acceptance_criteria>
  <done>
    handleLoad restaura mode, suretyPctStr e (em scenarios) appraisalStr + selectedDiscountPct.
    Itens legados carregam como modo fixo, fiança 0, sem erro.
    Round-trip save→load do modo cenários preserva o cenário escolhido (acceptance criterion 10 do SPEC).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificação visual e funcional fim a fim</name>
  <what-built>
    Toggle de modo, override de fiança, modo cenários completo (matriz desktop+mobile, click-to-select, zona ideal destacada), renomeação Total à vista→Custo inicial em todo lugar, persistência estendida, handleLoad com round-trip completo de cenários e backwards-compat para itens legados.
  </what-built>
  <how-to-verify>
    Rodar `npm run dev` e acessar a app. Validar:

    1. **Toggle:** topo do form mostra "Arremate fixo" / "Simular por avaliação" como segmented control. Clicar troca; recarregar a página mantém o último modo.
    2. **Trocar mode preserva estado:** preencha comissão=5, fiança=1, parcelas=30, comprador="X". Troque modos várias vezes — esses 4 campos NÃO podem zerar.
    3. **Settings:** abra Settings, configure "Carta de fiança padrão (%)" = 2, salve. Recarregue a app; novo cálculo deve abrir com fiança=2 (sublabel/placeholder/valor).
    4. **Override:** mude o campo de fiança principal para 5; calcule. Reabra Settings — default lá ainda é 2 (override não vaza pra Settings).
    5. **Modo cenários:** input "Valor de avaliação" = `R$ 61.000,00`, comissão=5, fiança=1, parcelas=30. Verifique a tabela:
       - Tem 13 linhas: 30, 35, 50, 55, 60, 65, 70, 75, 80, 85, 88, 90, 100
       - Linhas 70, 75, 80, 85 têm fundo destacado (zona ideal). Linha 65 e 88 NÃO têm destaque.
       - Linha 70%: Arrematação R$ 42.700,00, Entrada R$ 10.675,00, Saldo R$ 32.025,00, Comissão R$ 2.135,00, Fiança R$ 427,00, Custo inicial R$ 13.237,00, Parcela R$ 1.067,50
    6. **Click-to-select:** clique na linha 70% — painel Resumo popula, botões PDF/WhatsApp/Salvar habilitam. Clique na linha 50% — Resumo atualiza.
    7. **Mobile (<768px):** redimensione janela para ~375px. Tabela some, cards empilhados aparecem, mesmos valores, mesma zona destacada, click funciona.
    8. **Modo escuro + WCAG AA (gate dos tokens do Plan 01 Task 3):** toggle de tema. Realce de zona ideal continua legível em ambos os temas. Validar contraste ≥ 4.5:1 (DevTools → Inspect → Accessibility → Contrast, ou ferramenta tipo WebAIM Contrast Checker) entre `--zone-ideal-fg` e `--zone-ideal-bg`. Se REPROVAR em qualquer tema, retornar ao Plan 01 Task 3 e ajustar os tokens em `src/index.css` — não criar workaround inline aqui.
    9. **Salvar + Histórico (round-trip cenários — acceptance criterion 10 do SPEC):** preencha modo cenários com avaliação=61000, com=5, fia=1, 30x. Clique linha 70%. Salve. Abra Histórico — card mostra badge "Cenários (70%)". Clique "Carregar":
       - Modo volta automaticamente para "Simular por avaliação"
       - Campo "Valor de avaliação" preenchido com `R$ 61.000,00`
       - Linha 70% aparece selecionada (ring/border destacado)
       - Painel Resumo mostra os mesmos valores do save (Arrematação 42700, Custo inicial 13237, Parcela 1067.50)
       - Fiança no grid mostra `1`
       Salve um cálculo do modo fixo — badge "Arremate fixo". Carregue — modo volta para fixo, arremate populado.
    10. **Backwards-compat de itens legados:** se houver itens no histórico anteriores a esta fase (sem `mode`/`suretyPct` no calc), carregar NÃO pode crashar. Modo deve cair em "fixed", fiança em `0`. Se não houver itens legados, este check é N/A.
    11. **PDF + WhatsApp:** gere PDF de qualquer cálculo. Confira: hero label = "Custo inicial", existe linha "Carta de fiança: R$ X,XX". Gere WhatsApp — mesma checagem. Não deve aparecer "Total à vista" em lugar nenhum.
    12. **Sanity grep:** `grep -ri "Total à vista" src/` retorna ZERO ocorrências.

    Reporte qualquer divergência (valores numéricos errados, layout quebrado, contraste WCAG ruim, estado perdido na troca de modo, round-trip do histórico incompleto).
  </how-to-verify>
  <resume-signal>Digite "approved" quando todos os 12 checks passarem, ou liste os pontos a corrigir (em particular: contraste WCAG → Plan 01 Task 3; round-trip incompleto → Plan 05 Task 3).</resume-signal>
</task>

</tasks>

<verification>
- Caso canônico (61000/5/1/30) linha 70% bate com SPEC R3 visualmente
- grep -ri "Total à vista" src/ → 0
- Mode persiste em localStorage
- Settings default lido + override aplicado
- Histórico badge correto para ambos os modos
- handleLoad: round-trip completo do modo cenários (acceptance criterion 10) + backwards-compat para itens legados
- Contraste WCAG AA dos tokens de zona ideal validado em ambos os temas
</verification>

<success_criteria>
Todos os 11 acceptance criteria do SPEC.md passam:
- [ ] calculate() aceita suretyPct para 0/5/10 (Plan 01 Task 1, Test 5 cobre 10%)
- [ ] Settings persiste defaultSuretyPct e app usa
- [ ] Toggle visível, sem perda de meta/comissão/parcelas
- [ ] Matriz com exatamente 13 linhas em ordem locked
- [ ] Linhas 70-85 com destaque visual
- [ ] Click na linha popula Resumo + habilita botões
- [ ] 7 valores da linha 70% conferem
- [ ] grep "Total à vista" → 0
- [ ] PDF + WhatsApp com "Custo inicial" + linha "Carta de fiança"
- [ ] Histórico salva E recarrega cenário com mode/discountPct/suretyPct/appraisal (Task 3 desta plan)
- [ ] Layout 360px usável; modo escuro WCAG AA (validado em Task 4 item 8)
</success_criteria>

<output>
After completion, create `.planning/phases/01-cenarios-e-carta-fianca/01-05-SUMMARY.md`
</output>
