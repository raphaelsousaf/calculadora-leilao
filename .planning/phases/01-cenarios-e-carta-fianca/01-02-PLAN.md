---
phase: 01-cenarios-e-carta-fianca
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/schema.sql
  - supabase/migrations/0001_add_default_surety_pct.sql
  - src/lib/data.js
autonomous: false
requirements: [R2]
user_setup:
  - service: supabase
    why: "Adicionar coluna default_surety_pct em auctioneer_settings"
    dashboard_config:
      - task: "Rodar migration SQL no SQL Editor"
        location: "Supabase Dashboard → SQL Editor → cole o conteúdo de supabase/migrations/0001_add_default_surety_pct.sql → RUN"
must_haves:
  truths:
    - "Tabela auctioneer_settings tem coluna default_surety_pct (numeric, nullable)"
    - "fetchSettings retorna defaultSuretyPct quando presente; null caso contrário"
    - "upsertSettings persiste defaultSuretyPct"
  artifacts:
    - path: "supabase/migrations/0001_add_default_surety_pct.sql"
      provides: "DDL da nova coluna"
      contains: "default_surety_pct"
    - path: "supabase/schema.sql"
      provides: "schema atualizado"
      contains: "default_surety_pct"
    - path: "src/lib/data.js"
      provides: "fetchSettings/upsertSettings com novo campo"
      contains: "default_surety_pct"
  key_links:
    - from: "src/lib/data.js"
      to: "supabase auctioneer_settings"
      via: "select/upsert payload"
      pattern: "default_surety_pct"
---

<objective>
Adicionar coluna `default_surety_pct` à tabela `auctioneer_settings` e estender os wrappers `fetchSettings`/`upsertSettings` para persistir o default de carta de fiança configurado pelo usuário (R2).

Purpose: SettingsModal e App.jsx (Plans 03 e 05) precisam ler/escrever este campo. Migration tem que ser explícita porque a tabela tem colunas tipadas — não é JSON genérico.
Output: Migration SQL + schema atualizado + data.js extendido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-cenarios-e-carta-fianca/01-SPEC.md
@.planning/phases/01-cenarios-e-carta-fianca/01-CONTEXT.md
@supabase/schema.sql
@src/lib/data.js

<interfaces>
<!-- src/lib/data.js — assinaturas atuais e alvo -->
```js
// Atual
fetchSettings(userId) -> { nome, telefone, email, documento } | {}
upsertSettings(userId, { nome, telefone, email, documento }) -> void

// Alvo
fetchSettings(userId) -> { nome, telefone, email, documento, defaultSuretyPct } | {}
// defaultSuretyPct: number | null  (null se nunca configurado; consumidor aplica fallback 1)
upsertSettings(userId, { nome, telefone, email, documento, defaultSuretyPct }) -> void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration SQL + atualizar schema.sql</name>
  <files>supabase/migrations/0001_add_default_surety_pct.sql, supabase/schema.sql</files>
  <read_first>
    - supabase/schema.sql (estrutura atual)
  </read_first>
  <action>
    Criar `supabase/migrations/0001_add_default_surety_pct.sql` contendo:

    ```sql
    -- Phase 1: cenarios-e-carta-fianca
    -- Adiciona default de carta de fiança nas configurações do leiloeiro
    alter table public.auctioneer_settings
      add column if not exists default_surety_pct numeric;
    ```

    Editar `supabase/schema.sql` adicionando a linha `default_surety_pct numeric,` dentro do CREATE TABLE de `auctioneer_settings`, logo após `documento text,` e antes de `updated_at`. Manter `if not exists` no create (idempotência).
  </action>
  <verify>
    <automated>grep -c "default_surety_pct" supabase/schema.sql supabase/migrations/0001_add_default_surety_pct.sql</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/0001_add_default_surety_pct.sql exists
    - grep -c "default_surety_pct numeric" supabase/migrations/0001_add_default_surety_pct.sql >= 1
    - grep -c "default_surety_pct" supabase/schema.sql >= 1
    - grep -c "alter table public.auctioneer_settings" supabase/migrations/0001_add_default_surety_pct.sql >= 1
  </acceptance_criteria>
  <done>Migration file criado; schema.sql espelha o estado pós-migration.</done>
</task>

<task type="auto">
  <name>Task 2: Estender data.js com defaultSuretyPct</name>
  <files>src/lib/data.js</files>
  <read_first>
    - src/lib/data.js
  </read_first>
  <action>
    Per R2: editar `fetchSettings` para incluir `default_surety_pct` no `.select(...)` e mapear para `defaultSuretyPct` no retorno (camelCase). Editar `upsertSettings` para aceitar `s.defaultSuretyPct` no input e enviar `default_surety_pct: s.defaultSuretyPct ?? null` no payload.

    Mapeamento explícito (não retornar a coluna raw com snake_case). Exemplo:
    ```js
    const { data } = await supabase.from('auctioneer_settings')
      .select('nome, telefone, email, documento, default_surety_pct')
      .eq('user_id', userId).maybeSingle()
    return data ? {
      nome: data.nome, telefone: data.telefone, email: data.email,
      documento: data.documento, defaultSuretyPct: data.default_surety_pct
    } : {}
    ```
  </action>
  <verify>
    <automated>grep -c "defaultSuretyPct\\|default_surety_pct" src/lib/data.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "default_surety_pct" src/lib/data.js >= 2 (select + upsert payload)
    - grep -c "defaultSuretyPct" src/lib/data.js >= 2 (return mapping + upsert input)
    - npm run build não falha
  </acceptance_criteria>
  <done>fetchSettings retorna defaultSuretyPct; upsertSettings aceita defaultSuretyPct.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Usuário roda migration no Supabase</name>
  <what-built>Migration SQL `supabase/migrations/0001_add_default_surety_pct.sql` pronta para aplicar.</what-built>
  <how-to-verify>
    1. Abra o Supabase Dashboard do projeto
    2. Vá em SQL Editor → New query
    3. Copie e cole o conteúdo de `supabase/migrations/0001_add_default_surety_pct.sql`
    4. Clique em RUN — deve completar sem erro (a coluna ficará nullable; rows existentes terão `default_surety_pct = NULL`)
    5. Confira em Table Editor → auctioneer_settings que a coluna `default_surety_pct` existe
  </how-to-verify>
  <resume-signal>Digite "applied" quando a migration estiver aplicada, ou descreva qualquer erro do SQL Editor.</resume-signal>
</task>

</tasks>

<verification>
- Migration aplicada no Supabase (manual)
- data.js estende campos sem quebrar build
</verification>

<success_criteria>
- Coluna `default_surety_pct` existe em produção
- fetchSettings/upsertSettings handle o campo end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-cenarios-e-carta-fianca/01-02-SUMMARY.md`
</output>
