---
phase: 01-cenarios-e-carta-fianca
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/components/SettingsModal.jsx
autonomous: true
requirements: [R2]
must_haves:
  truths:
    - "SettingsModal exibe campo 'Carta de fiança padrão (%)'"
    - "Valor digitado é enviado em handleSaveSettings como defaultSuretyPct (number ou null)"
    - "Campo aceita 0 sem ser tratado como vazio (D-10)"
  artifacts:
    - path: "src/components/SettingsModal.jsx"
      provides: "Campo de fiança default"
      contains: "Carta de fiança"
  key_links:
    - from: "src/components/SettingsModal.jsx"
      to: "upsertSettings"
      via: "onSave({ ..., defaultSuretyPct })"
      pattern: "defaultSuretyPct"
---

<objective>
Adicionar campo "Carta de fiança padrão (%)" no SettingsModal, persistido via `upsertSettings` (R2, D-10). Valor `0` é válido e diferente de vazio.

Purpose: Sem este campo, usuário não consegue configurar default — App.jsx cai sempre no fallback 1%.
Output: SettingsModal com novo input numérico integrado ao save handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-cenarios-e-carta-fianca/01-SPEC.md
@.planning/phases/01-cenarios-e-carta-fianca/01-CONTEXT.md
@src/components/SettingsModal.jsx
@src/lib/data.js
@src/lib/format.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Campo "Carta de fiança padrão (%)" em SettingsModal</name>
  <files>src/components/SettingsModal.jsx</files>
  <read_first>
    - src/components/SettingsModal.jsx (estrutura: props, estado, layout, save handler)
    - src/lib/data.js (assinatura nova de fetchSettings/upsertSettings após Plan 02)
    - .planning/phases/01-cenarios-e-carta-fianca/01-CONTEXT.md (D-10 — sublabel canônico)
  </read_first>
  <action>
    Per R2 + D-10: adicionar input numérico "Carta de fiança padrão (%)" no SettingsModal, posicionado após o campo `documento` (próximo aos demais settings).

    Comportamento exato:
    - Estado interno: `const [defaultSuretyPctStr, setDefaultSuretyPctStr] = useState(...)` — inicializado de `initial.defaultSuretyPct` (vindo de fetchSettings). Se `null/undefined`, string vazia. Se número, `String(value)`.
    - Input type="number", step="0.1", min="0", max="100", placeholder="1"
    - Sublabel: "Use 0 se o edital não exigir." (texto canônico, não variar)
    - No save handler existente, incluir no payload: `defaultSuretyPct: defaultSuretyPctStr.trim() === '' ? null : Number(defaultSuretyPctStr)`. String vazia → null (apaga default), `'0'` → 0 (válido).
    - Validar: se Number(...) for NaN ou fora de [0, 100], não chamar save e exibir mensagem de erro inline (mesmo padrão dos demais campos do modal se houver).

    NÃO usar `parseBRL` — este é percentual simples, não BRL.
  </action>
  <verify>
    <automated>grep -c "defaultSuretyPct\\|Carta de fiança padrão" src/components/SettingsModal.jsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Carta de fiança padrão" src/components/SettingsModal.jsx >= 1
    - grep -c "defaultSuretyPct" src/components/SettingsModal.jsx >= 3 (estado + init + save payload)
    - grep -c "Use 0 se o edital não exigir" src/components/SettingsModal.jsx >= 1
    - npm run build passa
  </acceptance_criteria>
  <done>Campo aparece no modal; valor persiste via upsertSettings; recarrega corretamente.</done>
</task>

</tasks>

<verification>
- Build passa
- Campo visível no modal
- Save chama upsertSettings com defaultSuretyPct
</verification>

<success_criteria>
- Usuário consegue configurar default de fiança e ver persistido após reload
</success_criteria>

<output>
After completion, create `.planning/phases/01-cenarios-e-carta-fianca/01-03-SUMMARY.md`
</output>
