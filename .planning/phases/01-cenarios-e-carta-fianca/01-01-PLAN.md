---
phase: 01-cenarios-e-carta-fianca
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/calc.js
  - src/lib/calc.test.js
  - src/lib/scenarios.js
  - src/lib/scenarios.test.js
  - src/index.css
autonomous: true
requirements: [R1, R3]
must_haves:
  truths:
    - "calculate() aceita suretyPct e retorna surety com upfront = entry+commission+surety"
    - "calculate() é testado explicitamente para suretyPct=0, 5 e 10 (acceptance criterion 1 do SPEC)"
    - "buildScenarioMatrix(appraisal, commissionPct, suretyPct, installments) retorna 13 linhas com os percentuais default"
    - "Caso canônico (avaliação 61000, com 5%, fia 1%, 30x, linha 70%) bate com os 7 valores do SPEC"
    - "CSS tokens --zone-ideal-bg e --zone-ideal-fg existem para tema claro e escuro"
  artifacts:
    - path: "src/lib/calc.js"
      provides: "calculate() com suretyPct/surety"
      contains: "suretyPct"
    - path: "src/lib/scenarios.js"
      provides: "buildScenarioMatrix() + DEFAULT_SCENARIO_PCTS + IDEAL_ZONE"
      contains: "buildScenarioMatrix"
    - path: "src/index.css"
      provides: "tokens da zona ideal"
      contains: "--zone-ideal-bg"
  key_links:
    - from: "src/lib/scenarios.js"
      to: "src/lib/calc.js"
      via: "import { calculate }"
      pattern: "from ['\"]\\./calc"
---

<objective>
Estender a camada pura de cálculo com carta de fiança (R1) e introduzir a fábrica da matriz de 13 cenários (R3), além de adicionar os tokens CSS da zona ideal (D-09). Esta é a base que todos os outros planos consomem.

Purpose: Sem isto, nada do resto da fase compila numericamente correto. Manter `calculate` puro e síncrono, sem efeito colateral.
Output: `calc.js` estendido, `scenarios.js` novo com matriz, tokens CSS validados, todos com testes unitários.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-cenarios-e-carta-fianca/01-SPEC.md
@.planning/phases/01-cenarios-e-carta-fianca/01-CONTEXT.md
@src/lib/calc.js
@src/lib/format.js
@src/index.css

<interfaces>
<!-- Atual em src/lib/calc.js -->
```js
// Assinatura atual (será estendida)
export function calculate({ arremate, commissionPct, installments }):
  { bid, commissionPct, installments, entry, commission, upfront, remaining, installment, total }
```

<!-- Nova assinatura alvo -->
```js
export function calculate({ arremate, commissionPct, suretyPct, installments }):
  { bid, commissionPct, suretyPct, installments,
    entry, commission, surety, upfront, remaining, installment, total }
// upfront = entry + commission + surety
// surety  = bid * (suretyPct / 100)
// suretyPct ausente/undefined => 0 (backwards-compat)
```

<!-- Novo módulo src/lib/scenarios.js -->
```js
export const DEFAULT_SCENARIO_PCTS = [30,35,50,55,60,65,70,75,80,85,88,90,100]
export const IDEAL_ZONE = { min: 70, max: 85 } // inclusive ambos
export function buildScenarioMatrix({ appraisal, commissionPct, suretyPct, installments }):
  Array<{ pct, bid, entry, remaining, commission, surety, upfront, installment, isIdeal }>
// Cada linha: pct é o % de desconto da matriz; bid = appraisal * (pct/100)
// Demais valores via calculate()
// isIdeal = pct >= 70 && pct <= 85
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender calculate() com suretyPct/surety</name>
  <files>src/lib/calc.js, src/lib/calc.test.js</files>
  <read_first>
    - src/lib/calc.js (estado atual)
    - .planning/phases/01-cenarios-e-carta-fianca/01-SPEC.md (R1, acceptance criterion 1: "calculate() aceita suretyPct e retorna surety corretamente para 0%, 5% e 10%")
  </read_first>
  <behavior>
    - Test 1: calculate({ arremate: 61000, commissionPct: 5, suretyPct: 5, installments: 30 }) retorna surety === 3050 e upfront === entry + commission + 3050
    - Test 2: calculate({ arremate: 61000, commissionPct: 5, suretyPct: 0, installments: 30 }) retorna surety === 0 e upfront === entry + commission
    - Test 3: calculate sem suretyPct (undefined) retorna surety === 0 (backwards-compat)
    - Test 4: caso canônico linha 70% — calculate({ arremate: 42700, commissionPct: 5, suretyPct: 1, installments: 30 }) retorna entry=10675, commission=2135, surety=427, upfront=13237, remaining=32025, installment=1067.5
    - Test 5 (acceptance criterion 1 do SPEC, 10%): calculate({ arremate: 61000, commissionPct: 5, suretyPct: 10, installments: 30 }) retorna surety === 6100 e upfront === entry + commission + 6100
  </behavior>
  <action>
    Per R1 + D-10: editar `src/lib/calc.js` para aceitar `suretyPct` no param object. Computar `surety = bid * (sPct / 100)` onde `sPct = Number(suretyPct) || 0`. Atualizar `upfront = entry + commission + surety`. Incluir `surety` e `suretyPct: sPct` no objeto retornado. Manter pureza (sem I/O, sem Date, sem random). Criar `src/lib/calc.test.js` com Vitest cobrindo os 5 cenários do bloco `<behavior>` — Test 5 garante cobertura explícita do valor 10% que o acceptance criterion 1 do SPEC exige.

    Se Vitest não estiver instalado no projeto, adicionar como devDependency: `npm i -D vitest` e adicionar script `"test": "vitest run"` em package.json.
  </action>
  <verify>
    <automated>npm test -- calc.test 2>&1 | grep -E "(passed|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^#' src/lib/calc.js | grep -c "suretyPct" >= 2
    - grep -v '^#' src/lib/calc.js | grep -c "surety" >= 3
    - grep -c "entry + commission + surety" src/lib/calc.js >= 1
    - npm test src/lib/calc.test.js exits 0 com 5 testes passing
    - calculate({ arremate: 42700, commissionPct: 5, suretyPct: 1, installments: 30 }).upfront === 13237
    - calculate({ arremate: 61000, commissionPct: 5, suretyPct: 10, installments: 30 }).surety === 6100
  </acceptance_criteria>
  <done>calc.js estende com fiança preservando backwards-compat; testes cobrem 0%, 5% e 10% (acceptance criterion 1 do SPEC) + caso canônico.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar scenarios.js com buildScenarioMatrix</name>
  <files>src/lib/scenarios.js, src/lib/scenarios.test.js</files>
  <read_first>
    - src/lib/calc.js (assinatura nova após Task 1)
    - .planning/phases/01-cenarios-e-carta-fianca/01-SPEC.md (R3 — caso canônico)
    - .planning/phases/01-cenarios-e-carta-fianca/01-CONTEXT.md (specifics — 13 percentuais)
  </read_first>
  <behavior>
    - Test 1: DEFAULT_SCENARIO_PCTS é array com exatamente os 13 valores [30,35,50,55,60,65,70,75,80,85,88,90,100] nesta ordem
    - Test 2: IDEAL_ZONE === { min: 70, max: 85 }
    - Test 3: buildScenarioMatrix({ appraisal: 61000, commissionPct: 5, suretyPct: 1, installments: 30 }) retorna array com 13 linhas
    - Test 4: linha onde pct === 70 tem bid=42700, entry=10675, remaining=32025, commission=2135, surety=427, upfront=13237, installment=1067.5, isIdeal=true
    - Test 5: linha pct=30 tem isIdeal=false; pct=85 tem isIdeal=true; pct=88 tem isIdeal=false
    - Test 6: appraisal=0 retorna 13 linhas com bid=0 e demais valores 0 (não crash)
  </behavior>
  <action>
    Per R3 + D-09: criar `src/lib/scenarios.js` exportando:
    - `DEFAULT_SCENARIO_PCTS = [30,35,50,55,60,65,70,75,80,85,88,90,100]` (locked, não configurável)
    - `IDEAL_ZONE = { min: 70, max: 85 }`
    - `buildScenarioMatrix({ appraisal, commissionPct, suretyPct, installments })` que mapeia DEFAULT_SCENARIO_PCTS gerando para cada `pct`: `bid = appraisal * (pct/100)` e chama `calculate({ arremate: bid, commissionPct, suretyPct, installments })` para extrair entry/remaining/commission/surety/upfront/installment. Anexa `pct` e `isIdeal = pct >= 70 && pct <= 85`. Retorna array.

    Criar `src/lib/scenarios.test.js` cobrindo os 6 cases do `<behavior>`.
  </action>
  <verify>
    <automated>npm test -- scenarios.test 2>&1 | grep -E "(passed|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/scenarios.js exists
    - grep -c "DEFAULT_SCENARIO_PCTS" src/lib/scenarios.js >= 1
    - grep -c "buildScenarioMatrix" src/lib/scenarios.js >= 1
    - grep -E "30.*35.*50.*55.*60.*65.*70.*75.*80.*85.*88.*90.*100" src/lib/scenarios.js
    - grep -c "IDEAL_ZONE" src/lib/scenarios.js >= 1
    - npm test src/lib/scenarios.test.js exits 0 com 6 testes passing
  </acceptance_criteria>
  <done>scenarios.js exporta matriz com 13 linhas; caso canônico bate exato; testes passam.</done>
</task>

<task type="auto">
  <name>Task 3: Tokens CSS da zona ideal</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css (entender pattern de tokens CSS variables existentes — `:root` claro e seletor `.dark` ou similar)
    - .planning/phases/01-cenarios-e-carta-fianca/01-CONTEXT.md (D-09)
  </read_first>
  <action>
    Per D-09: adicionar duas CSS variables novas — `--zone-ideal-bg` e `--zone-ideal-fg` — seguindo o pattern existente de tokens (rgb triplets). Adicionar pares para tema claro e tema escuro nos mesmos seletores onde os outros tokens vivem.

    Valores propostos INICIAIS (a validação de contraste WCAG AA — texto sobre fundo deve atingir 4.5:1 — é feita pelo checkpoint humano em Plan 05 Task 3, item 8):
    - Tema claro: `--zone-ideal-bg: 254 243 199` (amber-100), `--zone-ideal-fg: 120 53 15` (amber-900)
    - Tema escuro: `--zone-ideal-bg: 69 26 3` (amber-950), `--zone-ideal-fg: 252 211 77` (amber-300)

    NÃO adicionar utility classes Tailwind aqui; o consumo será via inline `style={{ background: 'rgb(var(--zone-ideal-bg))' }}` ou similar no Plan 05.
  </action>
  <verify>
    <automated>grep -c "zone-ideal-bg" src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "\\-\\-zone-ideal-bg" src/index.css >= 2 (claro + escuro)
    - grep -c "\\-\\-zone-ideal-fg" src/index.css >= 2
    - npm run build (ou npm run dev) não falha
  </acceptance_criteria>
  <done>
    Tokens existem em ambos os temas; build não quebra.

    NOTA WCAG (importante): os valores acima são INICIAIS. O gate definitivo de WCAG AA (contraste ≥ 4.5:1 entre `--zone-ideal-fg` e `--zone-ideal-bg`, em ambos os temas) é o checkpoint humano em Plan 05 Task 3, item 8 ("Modo escuro: realce de zona ideal continua legível"). Se o checkpoint reprovar contraste, ajustar os tokens NESTE plano (src/index.css) e reexecutar — não criar workaround inline em Plan 05.
  </done>
</task>

</tasks>

<verification>
- `npm test` passa em calc.test.js e scenarios.test.js
- Acceptance criterion 1 do SPEC validado: testes explícitos para suretyPct 0%, 5% e 10%
- Caso canônico R$61000/5%/1%/30x → linha 70% bate com SPEC R3
- Tokens CSS existem para claro e escuro (contraste validado em Plan 05 Task 3)
</verification>

<success_criteria>
- calculate() backwards-compat (suretyPct undefined → surety=0)
- calculate() testado em 0%, 5% e 10% (cobre acceptance criterion 1 do SPEC integralmente)
- buildScenarioMatrix retorna exatamente 13 linhas, ordem locked
- Caso canônico verifier: ✓
- CSS tokens disponíveis para Plan 05 consumir (contraste WCAG AA validado em Plan 05 Task 3 item 8)
</success_criteria>

<output>
After completion, create `.planning/phases/01-cenarios-e-carta-fianca/01-01-SUMMARY.md`
</output>
