---
phase: 02-viabilidade-e-cronograma
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/scenarios.js
  - src/lib/scenarios.test.js
  - src/index.css
autonomous: true
requirements: [R6]

must_haves:
  truths:
    - "tierFor(pct, thresholds?) retorna excellent | good | marginal | over-market"
    - "Bordas inclusivas: pct <= T1 => excellent; pct <= T2 => good; pct <= T3 => marginal; senão over-market"
    - "Defaults aplicados quando thresholds omitidos: { t1: 70, t2: 85, t3: 95 }"
    - "TIER_LABELS exporta os 4 nomes em pt-BR"
    - "Tokens CSS --tier-marginal-* e --tier-over-market-* substituem caution/high-risk; lime --tier-good-* removidos"
    - "buildScenarioMatrix aceita thresholds e propaga para tierFor"
    - "Testes existentes passam com os novos nomes de tier"
  artifacts:
    - path: "src/lib/scenarios.js"
      provides: "tierFor com 4 tiers + thresholds opcionais; buildScenarioMatrix estendido"
      contains: "over-market"
    - path: "src/lib/scenarios.test.js"
      provides: "Tests cobrindo 4 tiers e thresholds custom"
      contains: "marginal"
    - path: "src/index.css"
      provides: "Tokens --tier-{excellent,good,marginal,over-market}-{bg,fg} em ambos temas"
      contains: "--tier-marginal-bg"
  key_links:
    - from: "src/lib/scenarios.js tierFor"
      to: "src/index.css --tier-{name}-bg/fg"
      via: "consumer (App.jsx) lê var(--tier-${row.tier}-bg)"
      pattern: "--tier-(excellent|good|marginal|over-market)-(bg|fg)"
---

<objective>
Refatorar `tierFor()` para 4 tiers configuráveis (`excellent | good | marginal | over-market`), atualizar tokens CSS e migrar testes.

Purpose: Source of truth única para classificação de viabilidade (D-12, D-13, D-14). Habilita Plans 02-04 (Settings) e 02-06 (UI).
Output: scenarios.js + scenarios.test.js + tokens CSS prontos para os Plans de Wave 2/3 consumirem.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-viabilidade-e-cronograma/02-SPEC.md
@.planning/phases/02-viabilidade-e-cronograma/02-CONTEXT.md
@src/lib/scenarios.js
@src/lib/scenarios.test.js
@src/index.css

<interfaces>
Estado atual `src/lib/scenarios.js`:
```js
export function tierFor(pct) {
  if (pct <= 35) return 'excellent'
  if (pct <= 65) return 'good'
  if (pct <= 85) return 'ideal'
  if (pct <= 90) return 'caution'
  return 'high-risk'
}
export const TIER_LABELS = { excellent, good, ideal, caution, 'high-risk' }
export const IDEAL_ZONE = { min: 70, max: 85 }
export function buildScenarioMatrix({ appraisal, commissionPct, suretyPct, installments }) { ... row.isIdeal = tier === 'ideal' }
```

Tokens atuais em `src/index.css` (claro): `--tier-excellent-{bg,fg}`, `--tier-good-{bg,fg}` (lime), `--tier-caution-{bg,fg}` (orange), `--tier-high-risk-{bg,fg}` (red), `--zone-ideal-{bg,fg}` (amber). Idem em `html.dark`.

App.jsx (consumer, NÃO mexer aqui — é Plan 02-06): hoje faz `const bg = row.tier === 'ideal' ? 'var(--zone-ideal-bg)' : 'var(--tier-${row.tier}-bg)'`. O Plan 02-06 simplificará isso usando os novos tokens.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refatorar tierFor + buildScenarioMatrix + TIER_LABELS</name>
  <files>src/lib/scenarios.js, src/lib/scenarios.test.js</files>
  <behavior>
    - tierFor(70) com defaults retorna 'excellent' (borda inclusiva em T1=70, per CONTEXT D-13/specifics)
    - tierFor(70.0001) retorna 'good'
    - tierFor(85) retorna 'good' (inclusive em T2=85)
    - tierFor(95) retorna 'marginal' (inclusive em T3=95)
    - tierFor(95.1) retorna 'over-market'
    - tierFor(30) retorna 'excellent'; tierFor(100) retorna 'over-market'
    - tierFor(70, { t1: 60, t2: 80, t3: 90 }) retorna 'good' (60 < 70 <= 80)
    - tierFor(50, { t1: 60, t2: 80, t3: 90 }) retorna 'excellent'
    - TIER_LABELS tem exatamente as 4 chaves: excellent="Excelente", good="Bom", marginal="Marginal", "over-market"="Acima do mercado"
    - buildScenarioMatrix({ ..., thresholds: { t1: 70, t2: 85, t3: 95 } }) retorna 13 linhas, linha pct=70 tem tier='excellent', pct=88 tem tier='marginal', pct=100 tem tier='over-market'
    - Caso canônico (avaliação=61000, com=5%, fia=1%, 30x) com defaults: linha 70% mantém bid=42700, commission=2135, surety=320.25, installment=1067.5, tier='excellent'
    - appraisal=0 não crasha, retorna 13 linhas com bid=0
  </behavior>
  <action>
    1. Em `src/lib/scenarios.js`:
       - Manter `DEFAULT_SCENARIO_PCTS` (não mexer).
       - Manter `IDEAL_ZONE` exportado por backwards-compat (valor `{ min: 70, max: 85 }`) MAS marcar deprecado com comentário JSDoc `@deprecated` — App.jsx ainda importa em Wave 1 antes do Plan 02-06 mudar.
       - Adicionar `export const DEFAULT_VIABILITY_THRESHOLDS = { t1: 70, t2: 85, t3: 95 }`.
       - Reescrever `tierFor(pct, thresholds)`:
         ```js
         export function tierFor(pct, thresholds = DEFAULT_VIABILITY_THRESHOLDS) {
           const t1 = Number(thresholds?.t1 ?? DEFAULT_VIABILITY_THRESHOLDS.t1)
           const t2 = Number(thresholds?.t2 ?? DEFAULT_VIABILITY_THRESHOLDS.t2)
           const t3 = Number(thresholds?.t3 ?? DEFAULT_VIABILITY_THRESHOLDS.t3)
           if (pct <= t1) return 'excellent'
           if (pct <= t2) return 'good'
           if (pct <= t3) return 'marginal'
           return 'over-market'
         }
         ```
       - Substituir `TIER_LABELS` por:
         ```js
         export const TIER_LABELS = {
           excellent: 'Excelente',
           good: 'Bom',
           marginal: 'Marginal',
           'over-market': 'Acima do mercado',
         }
         ```
       - Estender `buildScenarioMatrix({ appraisal, commissionPct, suretyPct, installments, thresholds })`:
         - passar `thresholds` para `tierFor(pct, thresholds)`
         - manter o campo `isIdeal` no retorno mas computar como `tier === 'good'` (era a antiga "ideal zone" 70-85, que agora é o tier `good`) para preservar o realce amber em consumers que ainda não migraram.

    2. Em `src/lib/scenarios.test.js`:
       - Atualizar imports: adicionar `tierFor, TIER_LABELS, DEFAULT_VIABILITY_THRESHOLDS`.
       - Test "isIdeal" existente: ajustar para o novo significado (tier `good` é a faixa 70-85 com defaults). Ou seja, pct=70 isIdeal=true, pct=85 isIdeal=true, pct=88 isIdeal=false, pct=30 isIdeal=false (era false antes também — confirmar).
         - **Atenção:** com bordas inclusivas em T1=70, pct=70 é tier `excellent`, NÃO `good`. Logo `isIdeal=false` para pct=70. Atualizar o test que hoje espera `isIdeal=true` em pct=70 para esperar o tier correto: `expect(row.tier).toBe('excellent')` e `expect(row.isIdeal).toBe(false)`.
       - Adicionar tests novos cobrindo todos os bullets do `<behavior>` acima (mínimo 1 test por bullet, podem ser agrupados em `describe('tierFor')` e `describe('TIER_LABELS')`).
       - Atualizar test "linha pct=70 bate o caso canônico": substituir `expect(row.isIdeal).toBe(true)` por `expect(row.tier).toBe('excellent')` e `expect(row.isIdeal).toBe(false)`. Valores numéricos (bid, commission, surety, installment) permanecem idênticos.
  </action>
  <verify>
    <automated>npx vitest run src/lib/scenarios.test.js</automated>
    Todos os testes devem passar.
  </verify>
  <done>scenarios.js exporta `tierFor` com 4 tiers + thresholds opcionais; TIER_LABELS tem 4 entradas; testes passam.</done>
</task>

<task type="auto">
  <name>Task 2: Renomear/atualizar tokens CSS para os 4 novos tiers</name>
  <files>src/index.css</files>
  <action>
    Em `src/index.css`, **em ambos `:root` (claro) e `html.dark` (escuro)**:

    1. Manter `--tier-excellent-bg` e `--tier-excellent-fg` como estão (verde).
    2. **Substituir** os tokens lime do antigo `good`:
       - Em `:root` trocar `--tier-good-bg: 236 252 203;` e `--tier-good-fg: 54 83 20;` por **alias do amber** existente:
         ```
         --tier-good-bg: 254 243 199; /* amber-100 — tier good (era ideal-zone) */
         --tier-good-fg: 120  53  15; /* amber-900 */
         ```
       - Em `html.dark` trocar para os mesmos amber-950/300:
         ```
         --tier-good-bg:  69  26   3; /* amber-950 */
         --tier-good-fg: 252 211  77; /* amber-300 */
         ```
       Manter `--zone-ideal-bg/fg` exatamente como está (alias compatível, App.jsx ainda referencia até Plan 02-06).
    3. **Renomear** `--tier-caution-*` → `--tier-marginal-*` (mesmos valores RGB orange):
       - `:root`: `--tier-marginal-bg: 255 237 213;` e `--tier-marginal-fg: 124 45 18;`
       - `html.dark`: `--tier-marginal-bg: 67 20 7;` e `--tier-marginal-fg: 253 186 116;`
    4. **Renomear** `--tier-high-risk-*` → `--tier-over-market-*` (mesmos valores RGB red):
       - `:root`: `--tier-over-market-bg: 254 226 226;` e `--tier-over-market-fg: 127 29 29;`
       - `html.dark`: `--tier-over-market-bg: 69 10 10;` e `--tier-over-market-fg: 252 165 165;`
    5. Atualizar comentários inline para refletir os novos nomes (`/* tier good (amber) */`, `/* tier marginal (orange) */`, `/* tier over-market (red) */`).
    6. Garantir que NÃO restam referências aos tokens antigos `--tier-caution-*` ou `--tier-high-risk-*` no arquivo.
  </action>
  <verify>
    <automated>grep -E "tier-(caution|high-risk)-" src/index.css | grep -v '^#' | wc -l</automated>
    Deve retornar 0 (zero referências aos nomes antigos).
    Também: `grep -c "tier-marginal-bg\|tier-over-market-bg" src/index.css` deve retornar >= 2 (claro + escuro, cada um 2 tokens — total >= 4 com -fg).
  </verify>
  <done>Tokens `--tier-{excellent,good,marginal,over-market}-{bg,fg}` definidos nos dois temas; `--zone-ideal-*` mantido como alias; nomes antigos `caution`/`high-risk` ausentes.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/scenarios.test.js` passa
- `grep -E "tier-(caution|high-risk)" src/index.css` (excluindo comentários) retorna 0
- `grep -c "over-market" src/lib/scenarios.js` retorna >= 1
- Caso canônico (61000/5%/1%/30x) na linha 70%: tier === 'excellent', bid=42700, surety=320.25 (numérico inalterado)
</verification>

<success_criteria>
- 4 tiers no source of truth: `excellent | good | marginal | over-market`
- Bordas inclusivas (`<=`) em T1, T2, T3
- Thresholds opcionais com defaults `{ t1: 70, t2: 85, t3: 95 }`
- Tokens CSS renomeados em ambos os temas mantendo contraste WCAG AA (mesmos RGB anteriores, só renomeados)
- Tests passam
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-viabilidade-e-cronograma/02-02-SUMMARY.md`.
</output>
