---
phase: 02-viabilidade-e-cronograma
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0002_add_viability_thresholds.sql
  - supabase/schema.sql
  - src/lib/data.js
autonomous: false
requirements: [R6]

must_haves:
  truths:
    - "auctioneer_settings tem 3 colunas viability_t1/t2/t3 (numeric, nullable)"
    - "fetchSettings retorna { viabilityT1, viabilityT2, viabilityT3 } camelCase"
    - "upsertSettings persiste os 3 thresholds quando informados"
  artifacts:
    - path: "supabase/migrations/0002_add_viability_thresholds.sql"
      provides: "Migration idempotente com 3 ALTER TABLE ADD COLUMN IF NOT EXISTS"
      contains: "viability_t1"
    - path: "supabase/schema.sql"
      provides: "Schema canônico com as 3 novas colunas em auctioneer_settings"
      contains: "viability_t1 numeric"
    - path: "src/lib/data.js"
      provides: "fetchSettings/upsertSettings mapeando snake_case <-> camelCase para os 3 thresholds"
      contains: "viability_t1"
  key_links:
    - from: "src/lib/data.js fetchSettings"
      to: "auctioneer_settings.viability_t1/t2/t3"
      via: "supabase.from('auctioneer_settings').select"
      pattern: "viability_t1.*viability_t2.*viability_t3"
---

<objective>
Adicionar 3 colunas `viability_t1`, `viability_t2`, `viability_t3` em `auctioneer_settings` (migration SQL + schema canônico) e estender `data.js` para mapear esses campos no fetch/upsert.

Purpose: Persistir os thresholds configuráveis de viabilidade (D-15, D-26, D-27) — viabiliza Plans 02-02 (refactor scenarios) e 02-04 (SettingsModal).
Output: Migration idempotente, schema atualizado, data.js estendido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-viabilidade-e-cronograma/02-SPEC.md
@.planning/phases/02-viabilidade-e-cronograma/02-CONTEXT.md
@supabase/migrations/0001_add_default_surety_pct.sql
@supabase/schema.sql
@src/lib/data.js

<interfaces>
Padrão atual de `fetchSettings` (src/lib/data.js):
```js
.select('nome, telefone, email, documento, default_surety_pct')
return data ? {
  nome: data.nome,
  telefone: data.telefone,
  email: data.email,
  documento: data.documento,
  defaultSuretyPct: data.default_surety_pct,
} : {}
```

Padrão da migration 0001:
```sql
alter table public.auctioneer_settings
  add column if not exists default_surety_pct numeric;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration 0002 + atualizar schema.sql</name>
  <files>supabase/migrations/0002_add_viability_thresholds.sql, supabase/schema.sql</files>
  <action>
    1. Criar `supabase/migrations/0002_add_viability_thresholds.sql` (per D-27) com header de comentário no mesmo padrão da 0001 e:
       ```sql
       alter table public.auctioneer_settings
         add column if not exists viability_t1 numeric,
         add column if not exists viability_t2 numeric,
         add column if not exists viability_t3 numeric;
       ```
       Idempotente. Pode rodar múltiplas vezes.
    2. Em `supabase/schema.sql`, dentro do bloco `create table if not exists public.auctioneer_settings (...)`, adicionar 3 colunas logo após `default_surety_pct numeric,` e antes de `updated_at`:
       ```
         viability_t1 numeric,
         viability_t2 numeric,
         viability_t3 numeric,
       ```
       Manter alinhamento com as colunas existentes.
  </action>
  <verify>
    <automated>grep -c "viability_t1" supabase/migrations/0002_add_viability_thresholds.sql supabase/schema.sql</automated>
    Deve retornar contagem >= 1 em cada arquivo. Também:
    `grep -E "add column if not exists viability_t[123]" supabase/migrations/0002_add_viability_thresholds.sql | wc -l` deve ser 3.
  </verify>
  <done>Migration 0002 existe com as 3 colunas; schema.sql atualizado refletindo o estado pós-migration.</done>
</task>

<task type="auto">
  <name>Task 2: Estender data.js para mapear viability_t1/t2/t3</name>
  <files>src/lib/data.js</files>
  <action>
    Em `src/lib/data.js` (per D-15, D-26):

    1. Em `fetchSettings`, adicionar `viability_t1, viability_t2, viability_t3` ao `.select(...)` (string única separada por vírgula):
       ```js
       .select('nome, telefone, email, documento, default_surety_pct, viability_t1, viability_t2, viability_t3')
       ```
    2. No retorno mapeado (quando `data` truthy), adicionar:
       ```js
       viabilityT1: data.viability_t1,
       viabilityT2: data.viability_t2,
       viabilityT3: data.viability_t3,
       ```
    3. Em `upsertSettings`, no objeto `payload`, adicionar:
       ```js
       viability_t1: s.viabilityT1 ?? null,
       viability_t2: s.viabilityT2 ?? null,
       viability_t3: s.viabilityT3 ?? null,
       ```
    Não adicionar validação numérica aqui — Settings cuida disso (Plan 02-04).
  </action>
  <verify>
    <automated>grep -c "viabilityT1\|viability_t1" src/lib/data.js</automated>
    Deve retornar >= 4 (select, fetch return, upsert payload key+value).
  </verify>
  <done>fetchSettings retorna `viabilityT1/T2/T3` em camelCase; upsertSettings persiste os 3 valores como `viability_t1/t2/t3`.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Aplicar migration 0002 no Supabase</name>
  <what-built>Migration SQL `supabase/migrations/0002_add_viability_thresholds.sql` adiciona 3 colunas idempotentes em `auctioneer_settings`.</what-built>
  <how-to-verify>
    1. Abrir o SQL Editor do projeto Supabase.
    2. Colar o conteúdo de `supabase/migrations/0002_add_viability_thresholds.sql`.
    3. Clicar RUN — comando deve completar sem erro (idempotente).
    4. Em Table Editor → `auctioneer_settings`, confirmar que as colunas `viability_t1`, `viability_t2`, `viability_t3` aparecem como `numeric` nullable.
  </how-to-verify>
  <resume-signal>Digite "applied" quando a migration estiver aplicada (ou "skipped" se já foi).</resume-signal>
</task>

</tasks>

<verification>
- `grep -c "viability_t1" supabase/schema.sql` retorna 1
- `grep -c "viabilityT1" src/lib/data.js` retorna pelo menos 1
- Migration 0002 contém os 3 ADD COLUMN IF NOT EXISTS
- Em produção (após Task 3), `select viability_t1, viability_t2, viability_t3 from auctioneer_settings limit 1` não dá erro
</verification>

<success_criteria>
- Schema canônico (`schema.sql`) reflete as 3 colunas novas
- Migration 0002 existe, é idempotente e segue o padrão da 0001
- `fetchSettings` retorna `viabilityT1/T2/T3` em camelCase
- `upsertSettings` persiste `viability_t1/t2/t3` em snake_case
- Migration aplicada no Supabase (Task 3)
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-viabilidade-e-cronograma/02-01-SUMMARY.md`.
</output>
