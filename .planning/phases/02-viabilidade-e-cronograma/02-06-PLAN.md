---
phase: 02-viabilidade-e-cronograma
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - src/App.jsx
autonomous: false
requirements: [R6, R7]

must_haves:
  truths:
    - "Grid principal de inputs cresce para 4 colunas em lg: Comissão / Carta de fiança / Parcelas / Valor de revenda"
    - "Campo 'Intervalo entre parcelas (dias)' aparece dentro do <details> 'Adicionar detalhes do arremate', ao lado da data do leilão"
    - "Cada linha da matriz e o painel Resumo recebem badge/cor das 4 faixas de viabilidade conforme thresholds salvos em Settings"
    - "Resumo mostra sublabel 'Margem estimada: R$ X (Y%)' quando revendaEsperada > 0; oculta quando 0/vazio"
    - "Matriz desktop ganha coluna 'Margem %' quando revendaEsperada > 0; oculta caso contrário"
    - "Cronograma exibido: card abaixo do Resumo no desktop (>=md); <details> dentro do Resumo no mobile"
    - "Sem dataLeilao preenchida, painel cronograma mostra placeholder 'Informe a data do leilão para gerar o cronograma'"
    - "Reclassificação imediata: alterar thresholds em Settings → matriz e badge atualizam sem reload"
    - "calc passado para PDF/WhatsApp inclui revendaEsperada e intervaloDias"
    - "handleLoad restaura revendaEsperada e intervaloDias do histórico"
    - "buildSavePayload inclui revendaEsperada e intervaloDias"
    - "tierFor é chamado com thresholds derivados de settings.viabilityT1/T2/T3 (defaults 70/85/95)"
  artifacts:
    - path: "src/App.jsx"
      provides: "UI completa de Fase 2 — revenda, intervalo, badge, cronograma, margem"
      contains: "revendaEsperada"
  key_links:
    - from: "src/App.jsx (Calculator)"
      to: "src/lib/scenarios.js tierFor"
      via: "buildScenarioMatrix({ ..., thresholds })"
      pattern: "thresholds:"
    - from: "src/App.jsx (Calculator)"
      to: "src/lib/schedule.js buildSchedule"
      via: "useMemo(() => buildSchedule(...), [...])"
      pattern: "buildSchedule"
    - from: "src/App.jsx handlePDF/handleWA"
      to: "src/lib/pdf.js + src/lib/whatsapp.js"
      via: "openPDF({ calc: { ...calc, revendaEsperada, intervaloDias }, meta, settings })"
      pattern: "revendaEsperada"
---

<objective>
Costurar tudo da Fase 2 na UI: campo revenda no grid principal, intervalo de parcelas no `<details>`, badge de viabilidade na matriz e Resumo, sublabel de margem, coluna 'Margem %', painel cronograma (card desktop + `<details>` mobile), persistência estendida.

Purpose: Plan terminal — entrega os 13 acceptance criteria do SPEC. Consome scenarios refatorado (02-02), schedule.js (02-03), data.js + thresholds persistidos (02-01, 02-04), e PDF/WhatsApp estendidos (02-05).
Output: App.jsx com UX completa da Fase 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-viabilidade-e-cronograma/02-SPEC.md
@.planning/phases/02-viabilidade-e-cronograma/02-CONTEXT.md
@src/App.jsx
@src/lib/scenarios.js
@src/lib/schedule.js
@src/lib/calc.js
@src/lib/format.js
@src/components/SettingsModal.jsx
@.planning/phases/02-viabilidade-e-cronograma/02-01-SUMMARY.md
@.planning/phases/02-viabilidade-e-cronograma/02-02-SUMMARY.md
@.planning/phases/02-viabilidade-e-cronograma/02-03-SUMMARY.md
@.planning/phases/02-viabilidade-e-cronograma/02-04-SUMMARY.md
@.planning/phases/02-viabilidade-e-cronograma/02-05-SUMMARY.md

<interfaces>
Após Plans 02-01..05, disponíveis:

```js
// src/lib/scenarios.js
export const DEFAULT_VIABILITY_THRESHOLDS // { t1: 70, t2: 85, t3: 95 }
export function tierFor(pct, thresholds)  // 'excellent'|'good'|'marginal'|'over-market'
export const TIER_LABELS                   // { excellent:'Excelente', good:'Bom', marginal:'Marginal', 'over-market':'Acima do mercado' }
export function buildScenarioMatrix({ appraisal, commissionPct, suretyPct, installments, thresholds })

// src/lib/schedule.js
export function buildSchedule(installments, dataLeilaoISO, intervaloDias = 30, valorParcela = 0)
// returns [{ n, dueDate, amount }]

// src/lib/format.js
export const brl, pct, formatBRLInput, parseBRL, formatDateBR

// settings shape (Plan 02-01): { ..., viabilityT1, viabilityT2, viabilityT3 }
```

Tokens CSS disponíveis (Plan 02-02): `--tier-{excellent,good,marginal,over-market}-{bg,fg}` em ambos temas.

Estado a adicionar:
- `revendaEsperadaStr: string` (formato BRL input, igual `arremateStr`)
- `intervaloDiasStr: string` (numérico inteiro)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estado, derivações e persistência (revenda, intervalo, thresholds)</name>
  <files>src/App.jsx</files>
  <action>
    Em `src/App.jsx`, dentro do componente `Calculator`:

    1. **Imports**: trocar `import { buildScenarioMatrix, IDEAL_ZONE } from './lib/scenarios'` por:
       ```js
       import { buildScenarioMatrix, tierFor, TIER_LABELS, DEFAULT_VIABILITY_THRESHOLDS } from './lib/scenarios'
       import { buildSchedule } from './lib/schedule'
       import { brl, formatBRLInput, formatDateBR, parseBRL, pct } from './lib/format'
       ```

    2. **Estados novos** (após `setSuretyTouched`):
       ```js
       const [revendaEsperadaStr, setRevendaEsperadaStr] = useState('')
       const [intervaloDiasStr, setIntervaloDiasStr] = useState('30')
       ```

    3. **Derivações** (junto aos useMemo existentes):
       ```js
       const revendaEsperada = useMemo(() => parseBRL(revendaEsperadaStr), [revendaEsperadaStr])

       const intervaloDias = useMemo(() => {
         const n = Number(intervaloDiasStr)
         if (!Number.isFinite(n) || n < 1) return 30
         return Math.min(365, Math.floor(n))
       }, [intervaloDiasStr])

       const thresholds = useMemo(() => ({
         t1: settings?.viabilityT1 ?? DEFAULT_VIABILITY_THRESHOLDS.t1,
         t2: settings?.viabilityT2 ?? DEFAULT_VIABILITY_THRESHOLDS.t2,
         t3: settings?.viabilityT3 ?? DEFAULT_VIABILITY_THRESHOLDS.t3,
       }), [settings])
       ```

    4. **Atualizar buildScenarioMatrix call** para passar thresholds:
       ```js
       const scenarios = useMemo(() => buildScenarioMatrix({
         appraisal, commissionPct, suretyPct, installments, thresholds,
       }), [appraisal, commissionPct, suretyPct, installments, thresholds])
       ```

    5. **Margem derivada** (inline no Resumo, mas calcular via useMemo para reuso):
       ```js
       const margin = useMemo(() => {
         if (revendaEsperada <= 0 || !hasValue) return null
         const totalEfetivo = (calc.bid || 0) + (calc.commission || 0) + (calc.surety || 0)
         const value = revendaEsperada - totalEfetivo
         const percent = (value / revendaEsperada) * 100
         return { value, percent }
       }, [revendaEsperada, calc.bid, calc.commission, calc.surety, hasValue])
       ```
       Mover essa declaração para depois de `hasValue` (que já existe).

    6. **Cronograma derivado**:
       ```js
       const schedule = useMemo(() => buildSchedule(
         calc.installments,
         meta?.dataLeilao,
         intervaloDias,
         calc.installment,
       ), [calc.installments, calc.installment, meta?.dataLeilao, intervaloDias])
       ```

    7. **Tier do cenário ativo** (para badge no Resumo):
       ```js
       const activeTier = useMemo(() => {
         if (mode === 'scenarios' && selectedDiscountPct != null) {
           return tierFor(selectedDiscountPct, thresholds)
         }
         if (mode === 'fixed' && hasValue && appraisal > 0) {
           return tierFor((effectiveArremate / appraisal) * 100, thresholds)
         }
         return null
       }, [mode, selectedDiscountPct, hasValue, appraisal, effectiveArremate, thresholds])
       ```

    8. **buildSavePayload**: adicionar `revendaEsperada` e `intervaloDias` ao retorno:
       ```js
       const base = {
         ...calc,
         mode,
         suretyPct,
         surety: calc.surety,
         revendaEsperada: revendaEsperada > 0 ? revendaEsperada : null,
         intervaloDias,
       }
       ```

    9. **handleLoad**: restaurar os 2 campos novos a partir de `c`:
       ```js
       setRevendaEsperadaStr(
         c.revendaEsperada && c.revendaEsperada > 0
           ? formatBRLInput(String(Math.round(c.revendaEsperada * 100)))
           : ''
       )
       setIntervaloDiasStr(c.intervaloDias != null ? String(c.intervaloDias) : '30')
       ```
       Inserir junto com os outros `setX(c.X)` existentes.

    10. **handleReset**: incluir reset dos 2 campos novos:
        ```js
        setRevendaEsperadaStr('')
        setIntervaloDiasStr('30')
        ```

    11. **handlePDF / handleWA**: enriquecer o `calc` passado para `openPDF` / `WhatsAppModal`. Criar helper local logo antes de `handlePDF`:
        ```js
        const calcForExport = useMemo(() => ({
          ...calc,
          revendaEsperada: revendaEsperada > 0 ? revendaEsperada : 0,
          intervaloDias,
        }), [calc, revendaEsperada, intervaloDias])
        ```
        Substituir `openPDF({ calc, meta, settings })` por `openPDF({ calc: calcForExport, meta, settings })`.
        No `<WhatsAppModal>` (já usa `calc={calc}`), trocar por `calc={calcForExport}`.

    Não adicionar libs novas. Sem mudanças em `src/components/WhatsAppModal.jsx` (passa `calc` adiante para `buildWhatsAppMessage` que Plan 02-05 já estendeu).
  </action>
  <verify>
    <automated>grep -c "revendaEsperada\|intervaloDias\|thresholds:\|buildSchedule" src/App.jsx</automated>
    Deve retornar >= 8 (múltiplas referências em estados, derivações, payload, load).
  </verify>
  <done>Estado, derivações e persistência prontos; calc enriquecido chega em PDF/WhatsApp.</done>
</task>

<task type="auto">
  <name>Task 2: UI — campo revenda no grid, intervalo no <details>, badge no Resumo, margem, cronograma, coluna Margem na matriz</name>
  <files>src/App.jsx</files>
  <action>
    Em `src/App.jsx`, dentro do JSX retornado por `Calculator`:

    1. **Grid de inputs (4 colunas em lg)**: localizar `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-5">` e trocar para `lg:grid-cols-4`. **Adicionar 4º item** após o bloco de "Parcelas":
       ```jsx
       <div className="flex flex-col">
         <div className="flex justify-between items-baseline gap-2 min-h-[20px]">
           <label className="label">Valor de revenda</label>
           <span className="text-[11px] text-fg-muted whitespace-nowrap">opcional</span>
         </div>
         <div className="mt-2 relative">
           <span className="absolute left-4 top-1/2 -translate-y-1/2 text-fg-subtle">R$</span>
           <input
             inputMode="decimal"
             className="input !pl-10 tabular-nums h-11"
             value={revendaEsperadaStr}
             onChange={e => setRevendaEsperadaStr(formatBRLInput(e.target.value))}
             placeholder="0,00"
           />
         </div>
         <p className="text-[11px] text-fg-muted mt-1.5">Esperado na revenda</p>
       </div>
       ```

    2. **Intervalo no `<details>` "Adicionar detalhes do arremate"**: localizar o `<div className="grid grid-cols-1 sm:grid-cols-2 gap-4">` que contém `Processo judicial` e `Data do leilão`. Substituir por `<div className="grid grid-cols-1 sm:grid-cols-3 gap-4">` e adicionar 3º Field:
       ```jsx
       <Field label="Intervalo entre parcelas (dias)">
         <input
           type="number" min="1" max="365" step="1"
           className="input"
           value={intervaloDiasStr}
           onChange={e => setIntervaloDiasStr(e.target.value)}
           placeholder="30"
         />
         <span className="text-xs text-fg-muted block mt-1">Estimado — confirme com o edital</span>
       </Field>
       ```
       Nota: o `Field` interno do App.jsx só aceita `label`+`children`; o sublabel vai como `<span>` filho.

    3. **Simplificar `tierStyle`** em `ScenariosMatrix` (já que Plan 02-02 manteve `--zone-ideal-*` como alias de amber, mas agora `--tier-good-*` também é amber):
       ```jsx
       const tierStyle = (row) => ({
         background: `rgb(var(--tier-${row.tier}-bg))`,
         color: `rgb(var(--tier-${row.tier}-fg))`,
       })
       ```
       (Remover o caso especial de `'ideal'`.)

    4. **Coluna 'Margem %' condicional na matriz desktop**: dentro do `<thead>` adicionar:
       ```jsx
       {revendaEsperada > 0 && (
         <th className="text-right px-3 py-2.5">Margem %</th>
       )}
       ```
       após `<th>Parcela</th>`. No `<tbody>` row mapping, adicionar:
       ```jsx
       {revendaEsperada > 0 && (
         <td className="px-3 py-2 text-right tabular-nums">
           {(() => {
             const totalEf = row.bid + row.commission + row.surety
             const m = revendaEsperada - totalEf
             const mp = (m / revendaEsperada) * 100
             return pct(mp)
           })()}
         </td>
       )}
       ```
       Para isso, `ScenariosMatrix` precisa receber `revendaEsperada` como prop. Atualizar a chamada `<ScenariosMatrix scenarios={scenarios} selected={selectedDiscountPct} onSelect={setSelectedDiscountPct} hasInput={appraisal > 0} />` adicionando `revendaEsperada={revendaEsperada}`. Atualizar a assinatura: `function ScenariosMatrix({ scenarios, selected, onSelect, hasInput, revendaEsperada })`.

       Nos cards mobile, adicionar dentro do `<div className="grid grid-cols-2 ...">` no final, condicional:
       ```jsx
       {revendaEsperada > 0 && (() => {
         const totalEf = row.bid + row.commission + row.surety
         const m = revendaEsperada - totalEf
         const mp = (m / revendaEsperada) * 100
         return (<>
           <span className="opacity-70">Margem %</span>
           <span className="text-right tabular-nums">{pct(mp)}</span>
         </>)
       })()}
       ```

    5. **Badge de viabilidade no Resumo**: dentro do `<div className="flex items-baseline justify-between gap-2">` que contém `<h2>Resumo</h2>`, adicionar (à direita do h2 e antes do span de commissionPct):
       ```jsx
       {activeTier && (
         <span
           className="text-[11px] uppercase tracking-[0.06em] font-semibold rounded-full px-2.5 py-1"
           style={{
             background: `rgb(var(--tier-${activeTier}-bg))`,
             color: `rgb(var(--tier-${activeTier}-fg))`,
           }}
         >
           {TIER_LABELS[activeTier]}
         </span>
       )}
       ```

    6. **Sublabel de margem no hero-stat "Custo inicial"**: localizar `<div className="text-xs mt-1.5 opacity-60">Entrada + comissão + carta de fiança</div>` e adicionar **abaixo**:
       ```jsx
       {margin && (
         <div
           className="text-xs mt-1 font-medium tabular-nums"
           style={{
             color: margin.value > 0
               ? 'rgb(var(--tier-excellent-fg))'
               : margin.value < 0
                 ? 'rgb(var(--tier-over-market-fg))'
                 : 'rgb(var(--fg-muted))',
           }}
         >
           Margem estimada: {brl(margin.value)} ({pct(margin.percent)})
         </div>
       )}
       ```

    7. **Painel cronograma**: criar componente local `ScheduleTable` (no mesmo arquivo) e renderizar com responsividade (per D-19/D-20/D-23):
       ```jsx
       function ScheduleTable({ schedule }) {
         const todayISO = new Date().toISOString().slice(0, 10)
         const nextIdx = schedule.findIndex(p => p.dueDate >= todayISO)
         return (
           <div className="overflow-x-auto rounded-xl border border-line">
             <table className="w-full text-sm tabular-nums">
               <thead className="bg-soft text-fg-muted text-[11px] uppercase tracking-[0.06em]">
                 <tr>
                   <th className="text-left px-3 py-2">#</th>
                   <th className="text-left px-3 py-2">Vencimento</th>
                   <th className="text-right px-3 py-2">Valor</th>
                 </tr>
               </thead>
               <tbody>
                 {schedule.map((p, idx) => {
                   const past = p.dueDate < todayISO
                   const isNext = idx === nextIdx
                   return (
                     <tr
                       key={p.n}
                       className={`border-t border-line ${past ? 'opacity-60' : ''} ${isNext ? 'bg-soft' : ''}`}
                     >
                       <td className="px-3 py-2">{p.n}</td>
                       <td className="px-3 py-2 flex items-center gap-2">
                         {isNext && <Icon name="calendar" className="w-3.5 h-3.5 text-accent" />}
                         {formatDateBR(p.dueDate)}
                       </td>
                       <td className="px-3 py-2 text-right">{brl(p.amount)}</td>
                     </tr>
                   )
                 })}
               </tbody>
             </table>
           </div>
         )
       }
       ```

       **Nota sobre `Icon name="calendar"`**: se o ícone não existir em `Icon`, usar `<Icon name="check" />` ou omitir o ícone (o comportamento de realce com `bg-soft` já cumpre o D-23). Verificar via `grep -n "calendar" src/components/Icon.jsx` antes de usar; se ausente, remover o ícone e manter apenas o realce de bg.

       **Renderização**:
       - **Desktop** (>=md): após o card de Resumo (i.e., após o `<p>` "Os valores são referenciais...") na seção de results, **fora** do `card` mas dentro da `<section>`, adicionar (per D-19):
         ```jsx
         <div className="hidden md:block card p-5 sm:p-7 mt-5">
           <h3 className="text-base font-semibold text-fg mb-3">Cronograma de pagamento</h3>
           {!meta.dataLeilao ? (
             <p className="text-sm text-fg-muted">
               Informe a data do leilão para gerar o cronograma.
             </p>
           ) : schedule.length === 0 ? (
             <p className="text-sm text-fg-muted">Cronograma indisponível.</p>
           ) : (
             <>
               <p className="text-xs text-fg-muted mb-3">
                 {schedule.length} parcelas · intervalo de {intervaloDias} dias · a partir de {formatDateBR(meta.dataLeilao)}
               </p>
               <ScheduleTable schedule={schedule} />
             </>
           )}
         </div>
         ```
       - **Mobile** (<md): dentro do `card` de Resumo, **antes** dos botões PDF/WhatsApp (i.e., antes do `<div className="mt-7 grid grid-cols-2 gap-2">`), adicionar:
         ```jsx
         <details className="md:hidden mt-5 rounded-xl border border-line bg-soft/40">
           <summary className="cursor-pointer select-none px-4 py-3 text-sm font-medium text-fg flex items-center justify-between">
             <span>Ver cronograma de pagamento {meta.dataLeilao && schedule.length ? `(${schedule.length} parcelas)` : ''}</span>
             <Icon name="chevron-down" className="w-4 h-4 text-fg-subtle" />
           </summary>
           <div className="px-4 pb-4 pt-2 border-t border-line">
             {!meta.dataLeilao ? (
               <p className="text-sm text-fg-muted">Informe a data do leilão para gerar o cronograma.</p>
             ) : schedule.length === 0 ? (
               <p className="text-sm text-fg-muted">Cronograma indisponível.</p>
             ) : (
               <ScheduleTable schedule={schedule} />
             )}
           </div>
         </details>
         ```
         Mover o aviso "Os valores são referenciais..." para permanecer **após** ambos os blocos (manter posição final).

    8. **Limpar import morto**: remover `IDEAL_ZONE` se ainda importado (não será usado).
  </action>
  <verify>
    <automated>grep -c "Valor de revenda\|Intervalo entre parcelas\|Cronograma de pagamento\|Margem estimada\|TIER_LABELS\|activeTier" src/App.jsx</automated>
    Deve retornar >= 6.
  </verify>
  <done>UI completa: revenda no grid (4 cols lg), intervalo no details, badge no Resumo, margem no hero-stat, coluna Margem % na matriz quando revenda > 0, cronograma desktop/mobile com placeholder.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificar end-to-end com caso canônico</name>
  <what-built>App.jsx integrado: revenda, intervalo, badge, cronograma, margem, coluna Margem %, persistência.</what-built>
  <how-to-verify>
    1. `npm run dev`. Modo "Simular por avaliação".
    2. Avaliação=R$ 61.000, Comissão=5%, Carta=1%, Parcelas=30, Revenda=R$ 58.000. Em "Adicionar detalhes do arremate": Data do leilão=2026-05-01, Intervalo=30.
    3. Selecionar a linha 70% na matriz.
    4. **Resumo:**
       - Badge "Excelente" (verde) à direita do título "Resumo".
       - Hero-stat "Custo inicial" R$ 13.130,25 (inalterado de Fase 1) com sublabel "Margem estimada: R$ 12.844,75 (22,15%)" em verde.
       - Mini-stats: Entrada R$ 10.675,00; Comissão (5%) R$ 2.135,00; Carta de fiança (1%) R$ 320,25; Saldo R$ 32.025,00.
       - Parcela 30x sem juros: R$ 1.067,50.
    5. **Matriz desktop:**
       - Linha 70% com cor verde (excellent), badge implícito por background.
       - Coluna nova "Margem %" presente; linha 70% mostra valor positivo; linha 100% mostra valor negativo.
       - Linhas 75/80/85 amber (good), 88/90 orange (marginal), 100 red (over-market).
    6. **Cronograma desktop:** card abaixo do Resumo com tabela, primeira linha "1 · 31/05/2026 · R$ 1.067,50", última "30 · 27/10/2028 · R$ 1.067,50".
    7. **Mobile (resize <768px):** cronograma vira `<details>` dentro do card de Resumo, padrão fechado, label "Ver cronograma de pagamento (30 parcelas)". Expandir mostra a mesma tabela.
    8. Limpar Data do leilão → cronograma mostra "Informe a data do leilão para gerar o cronograma." em ambos os layouts.
    9. Settings → mudar T1 para 65, T2 para 80, T3 para 90, salvar. Sem reload, a linha 70% deve mudar de verde para amber (good — pois 65 < 70 ≤ 80) e o badge do Resumo deve refletir.
    10. Limpar campo Revenda → sublabel de margem some, coluna "Margem %" some da matriz.
    11. Salvar cálculo no histórico → carregar de novo → revenda e intervalo voltam preenchidos.
    12. Gerar PDF → PDF tem linha "Margem estimada" no card e segunda página com "Cronograma de pagamento" listando 30 parcelas.
    13. Gerar WhatsApp → mensagem inclui "💰 Margem estimada: R$ 12.844,75 (22,15%)" e "📅 Primeira parcela: 31/05/2026 · Última: 27/10/2028". Total &lt; 1500 chars.
    14. Modo escuro → 4 cores das faixas mantêm contraste legível.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva o que falhou.</resume-signal>
</task>

</tasks>

<verification>
- `grep -c "revendaEsperada" src/App.jsx` >= 5
- `grep -c "intervaloDias" src/App.jsx` >= 4
- `grep -c "TIER_LABELS\[activeTier\]" src/App.jsx` >= 1
- Todos os 13 acceptance criteria do SPEC checados manualmente em Task 3
- `npm run build` passa sem erros (verificar antes do checkpoint)
</verification>

<success_criteria>
- Os 13 acceptance criteria do SPEC R6+R7 cumpridos
- Reclassificação imediata ao alterar thresholds em Settings
- Persistência de revendaEsperada e intervaloDias no histórico
- Cronograma e margem corretamente integrados a PDF e WhatsApp
- Modo escuro mantém contraste WCAG AA
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-viabilidade-e-cronograma/02-06-SUMMARY.md`.
</output>
